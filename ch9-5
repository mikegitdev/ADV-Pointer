#include <stdio.h>
#include <conio.h>
int main()
{
	int count = 0;
	char ar[20];
	for (int  i = 0; i < 20; i++)
	{
		scanf_s("%c", &ar[i]);
		count++;
		if (ar[i]=='#')
		{
			goto reverse;
		}
	}
	reverse: for (int i = count-2; i >=0; i--)
	{
		printf("%c", ar[i]);
		
	}





	_getch();
}
